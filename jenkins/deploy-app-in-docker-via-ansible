1)lainuch jenkins instance 

2)laiunch instance for and docker 

#1)jenkins==
  $ hostnamectl set-hostname jenkins
  $ /bin/bash ==login as jenkins shell

-apt install python3-pip -y -for pip
  -pip install docker ---for ansible docker depicuiency



 # 2)dockr 
-docker install 
-usermod -aG docker ubuntu && newgrp docker
-ssh-keygen 
  
#1) cd /etc/ansible
$ vim hosts
[dockerservers]
(ip) dockerserverwhere is it]

-passwd jenkins 
su jenkins

-ssh-keygen 
$ cd .ssh 

$ ssh-copy-id copy pub key into docker server  or cat id_rsa.pub
#(2)docker server 
>copy key and paste >open folder in .ssh (docker server) >vim authorzed_key >paste in authorized key 


1)# server 1 jenkins 

ssh root@docker_server_ip    and exit 

$ mkdor playbbokl 
$ cd playbook 

____________write here playbbok 








